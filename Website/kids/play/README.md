# WebGL Build Output

Use Unity to build Money Muncher into this folder:

`Money Muncher > Build Kids WebGL`

After the build, this folder should contain Unity's generated `index.html`, `Build/`, and `TemplateData/` files. Upload the contents of `Website/kids` to the `/kids` directory on `moneymuncher.ca`.
